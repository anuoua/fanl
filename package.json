{
  "name": "fanl",
  "version": "0.0.0",
  "type": "module",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "build": "bun run build.ts && bun run build:types",
    "build:types": "tsc -p tsconfig.build.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@app-route/core": "0.0.1-11"
  }
}
